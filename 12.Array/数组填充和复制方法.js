// fill
{
    {
        let arr = [0, 1, 2, 3, 4, 5];
        console.log('fill全部填充:', arr.fill('a')); // 全部填充
    }

    {
        let arr = [0, 1, 2, 3, 4, 5];
        console.log('fill指定开始位置：', arr.fill('a', 2)); // 从索引2开始到结束
    }
    {
        let arr = [0, 1, 2, 3, 4, 5];
        console.log('fill指定开始位置,结束位置：', arr.fill('a', 2, 4)); // 从索引2开始到4之前结束
    }

}
//copyWithin
{

}